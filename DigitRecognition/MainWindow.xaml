<Window x:Class="DigitRecognition.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DigitRecognition"
        xmlns:cnv="clr-namespace:DigitRecognition.Converters"
        mc:Ignorable="d"
        Title="{Binding WindowTitle, FallbackValue = Test Value}" 
        Height="450" 
        Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel
            Orientation="Horizontal"
            Height="30"
            Grid.Row="0">
            <Button 
                Command="{Binding ChangePage}"
                CommandParameter="0"
                Content="1"></Button>
            <Button 
                Command="{Binding ChangePage}"
                CommandParameter="1"
                Content="2"></Button>
        </StackPanel>
        <Frame
            Grid.Row="1"
            Content="{
            Binding CurrentPage, 
            Converter={cnv:ApplicationPageConverter}}">
        </Frame>
    </Grid>
</Window>
